# Evitar duplicados (por ejemplo, ID o nombre ya existente)
# Reglas específicas del sistema (por ejemplo, stock no negativo)
# Relación entre datos (por ejemplo, fecha de inicio antes que fecha final)